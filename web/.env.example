# API Configuration
# Leave empty to use relative URLs (same origin as web app)
# Or set to your API URL if different: http://localhost:8080
VITE_API_URL=

# App Configuration
VITE_APP_NAME=AWS Go Server
